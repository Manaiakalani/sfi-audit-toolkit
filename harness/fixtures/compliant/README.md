# Compliant sample project

A minimal fixture that models SFI-aligned engineering practices: managed
identity + Key Vault, pinned CI actions and dependencies, private networking,
CODEOWNERS, and a security policy. Credentials shown anywhere are placeholders.
